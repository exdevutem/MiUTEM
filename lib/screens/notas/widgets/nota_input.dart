import 'package:flutter/material.dart';

class NotaInput extends StatelessWidget {
  const NotaInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
