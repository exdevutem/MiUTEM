<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permisos que pide screengrab para las capturas de pantalla:
         https://docs.fastlane.tools/getting-started/android/screenshots/ -->

    <!-- Allows unlocking your device and activating its screen so UI tests can succeed -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Allows storing screenshots on external storage, where it can be accessed by ADB.
         Va sin el `android:maxSdkVersion="18"` con el que aparece en la guía: el plugin
         file_saver declara el mismo permiso con 28 y el merge de manifests corta el build
         con "Attribute uses-permission#...@maxSdkVersion value=(18) is also present at
         [:file_saver] value=(28)". Pisarlo con tools:replace le sacaría a file_saver el
         permiso en las APIs 19 a 28; dejarlo sin el atributo no le quita nada a nadie. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Allows changing locales -->
    <uses-permission
        android:name="android.permission.CHANGE_CONFIGURATION"
        tools:ignore="ProtectedPermissions" />

    <!-- Allows changing SystemUI demo mode -->
    <uses-permission
        android:name="android.permission.DUMP"
        tools:ignore="ProtectedPermissions" />
</manifest>
